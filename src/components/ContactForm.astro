---
import InputField from "./InputField.astro";
import SocialMidiaLinks from "./SocialMidiaLinks.astro";
---

<div class="contactForm py-8 md:py-12">
	<div class="container px-4 2xl:px-0">
		<section>

			<div class="contactForm__introduction">
				<h3 class="custom__title text-primary inline-block mb-4">
					Fale conosco
				</h3>

				<div class="contactForm__socialText">

					<p class="contactForm__title text-primary my-4 md:my-10">
						Entre em contato <span>conosco</span>
					</p>

					<SocialMidiaLinks />					
				</div>

			</div>

			<div class="contactForm__formContent py-8">
				<form name="frmContact">
					<div class="contactForm__inputWrap">
						<InputField label="Seu nome" type="text" id="inpt_name" />
						<InputField label="Endereço de e-mail" type="email" id="inpt_email" />
						<InputField label="Telefone (opcional)" type="text" id="inpt_telefone" />
					</div>

					<div class="textArea my-5">
						<textarea name="area_message" id="contacMessage" class="textArea__content"></textarea>				
						<label for="contacMessage" class="txtArea__label">Descreva da melhor forma qual seu objetivo em seu negócio</label>
						<span class="textArea__error">Campo obrigatório</span>
					</div>

					<div class="contactFrom__btnSubmit">
						<button class="btn bg-secondary text-white p-4 lg:p-8">
							<span class="inline-flex gap-4 items-center">
								Enviar mensagem <i class="icon-arrow-right"></i>
							</span>
						</button>
					</div>

				</form>
			</div>

		</section>

	</div>
</div>


<style lang="scss">

	.contactForm__title {
		font-weight: 800;
		font-size: 2rem;
		line-height: 1;

		span {
			display: block;
		}
	}

	.textArea {
		position: relative;	

		&__content {
			width: 100%;
			min-height: 250px;
			border: 0;
			border-bottom: 1px solid #cacaca;
			display: block;
			padding: 40px 10px 10px;
			background-color: transparent;
			line-height: 1.4;
			color: #262E32;

			&:focus {
				outline: none;
				border-color: var(--secondary-color);
			}

			~ label {
				position: absolute;
				left: 10px;
				width: 100%;
				top:-9px;
				color: #495861;
				transition: 0.3s;
				letter-spacing: 0.5px;
			}

			&:focus ~ label {
				top: -15px;
				font-size: 0.875rem;
				color: var(--secondary-color);
				transition: 0.3s;
			}
		}

		&__error {
			margin: 4px 0;
			font-size: .75rem;
			color: red;
			opacity: 0;
			visibility: hidden;
		}

		&.is__error {
			.inputField__error {
				opacity: 1;
				visibility: visible;
			}
		}
  	}

	.contactForm__socialText {
		position: relative;

		:global(.socialMidia) {
			position: absolute;
			top: 50%;
			transform: translateY(-50%);
			right: 0;
			display: flex;
			flex-direction: column;
			gap: 16px;		
			color: #1E2532;
		}

		:global(.socialMidia__item a){
			width: 35px;
			height: 35px;
			display: inline-flex;
			justify-content: center;
			align-items: center;
			border: 1px solid #B7B7B7;
			border-radius: 100%;	
		}
		:global(.socialMidia__icon::before){		
			color: #1E2532;
			font-size: 0.875rem;
		}
	}

	@media(screen(md)) {

		.contactForm {
			&__inputWrap {
				display: flex;
				flex-wrap: wrap;
				align-items: center;
				gap: 32px;

				:global(.inputField) {
					flex: 1 1 auto;
					margin: 16px 0;
				}
			}
		}
		.textArea {
		
			&__content {
				min-height: 145px;
				~ label {
					font-size: 1.2rem;
				}

				&:focus ~ label {
					top: -20px;		
					font-size: 1.2rem;		
				}
			}
		}
	}

	@media (screen(lg)) {
		.contactForm__title {
			font-size: 5rem;
		}

		.contactForm__socialText {

			:global(.socialMidia) {	
				gap: 30px;	
			}

			:global(.socialMidia__item a){
				width: 50px;
				height: 50px;				
			}

			:global(.socialMidia__icon::before){		
				font-size: 1rem;
			}
		}
	}
	
</style>
